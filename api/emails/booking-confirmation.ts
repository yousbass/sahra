// Explicit route for booking confirmation email (maps to dynamic handler)
// eslint-disable-next-line @typescript-eslint/no-var-requires
const handler = require('./[action]').default;

export default handler;
